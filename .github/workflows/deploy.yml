name: Auto Deploy to EC2

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          cd /home/ubuntu/tableorder
          git pull origin main
          cp config/.env.simple .env
          sed -i "s/ALLOWED_HOSTS=.*/ALLOWED_HOSTS=localhost,127.0.0.1,${{ secrets.EC2_HOST }}/" .env
          docker-compose down || true
          docker-compose up -d --build
          sleep 30
          curl -f http://localhost || echo "Warning: Health check failed"
    
    - name: Notify Success
      if: success()
      run: echo "🎉 배포 성공!"
    
    - name: Notify Failure  
      if: failure()
      run: echo "❌ 배포 실패!"